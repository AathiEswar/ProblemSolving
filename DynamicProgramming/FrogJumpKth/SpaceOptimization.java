package ProblemSolving.DynamicProgramming.FrogJumpKth;

public class SpaceOptimization {
    public int spaceOptimization(int[] arr , int k ){
        return 0;
        // we cannot optimize space ,
        // we will need K number of elements
        // worst case for K is N which is just normal tabulation
    }
}
